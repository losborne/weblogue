{% extends "base" %}
{% block title %}Weblogue{% endblock title %}

{% block content %}
  {% for post in posts %}
    <div class="post">
      <h2>{{ post.title }}</h2>
      <p>{{ post.body }}</p>
    </div>
  {% endfor %}
{% endblock content %}

